import { RegisterForm } from "@/components/auth/register-form";

export default function RegisterPage() {
    return (
        <main className="bg-red w-full h-full flex justify-center items-center p-4">
            <RegisterForm />
        </main>
    );
}