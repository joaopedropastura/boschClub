import { RegisterForm } from "@/components/auth/register-form";

export default function RegisterPage() {
    return (
        <main className="bg-red h-full flex items-center">
            <RegisterForm />
        </main>
    );
}